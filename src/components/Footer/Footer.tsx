import { FunctionComponent } from "react";
import styles from "./Footer.module.scss";

const Footer: FunctionComponent = () => {
  return <div className={styles.container}>Cím</div>;
};

export default Footer;
